#!/usr/bin/env perl
$kms = 0 ;
while ($kms < 50) {
    $kms += 1 ;
    if ($kms = 1) {
	print "$kms km à pied, ça use, ca use\n";
    }
    else {
	print "$kms kms à pied, ça use, ca use\n";
    }
	$kms += 1
}

